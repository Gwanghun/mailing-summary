# allow_senders.yaml
# ---------------------------------------------------------------
# 수동으로 뉴스레터로 지정할 발신자 목록
# python main.py add-sender --email foo@example.com 으로 추가
# 또는 직접 이 파일을 편집해도 됩니다.
# ---------------------------------------------------------------

# 특정 이메일 주소를 뉴스레터로 강제 지정
emails: []

# 특정 도메인을 뉴스레터로 강제 지정 (newsletter_sources.yaml 와 동일 역할)
# newsletter_sources.yaml 에 추가하기 전에 빠르게 테스트할 때 활용
domains: []
